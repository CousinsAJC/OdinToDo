const createTodo = function(title, desc, dueDate, priority, notes) {
    const todo = {
        "title" : title,
        "desc" : desc,
        "dueDate" : dueDate,
        "priority" : priority,
        "notes" : notes
    }

    //add todo to array or JSON or both
}

export { createTodo };